# Integrated Gradients Analysis

The integrated gradients analysis works with the one-beat dataset, which consists of 6044 subjects with consistent heartbeats. See `average_beat_pixel_removal.md` for a full explanation of how these 6044 subjects were selected. For each of those subjects, all heartbeats are averaged together to obtain a single representative heartbeat in each channel.

The analysis was implemented using the `IntegratedGradients` class in the `captum` package. Integrated gradients were introduced in 2017 as a way to identify which pixels of an image (or any input) contribute most strongly to the output generated by a model @sundararajan_axiomatic_2017. In order to calculate integrated gradients, we start with a baseline input $x'$. @sundararajan_axiomatic_2017 recommends using a black image or zero embedding for image and text analysis, respectively. We use a flatline heartbeat as our baseline input. Once we have a baseline, integrated gradients are calculated for a pixel $i$ in an input vector $x$ using the formula

$$IntegratedGrads_i(x) = (x_i - x'_i) \int_0^1 \frac{\partial F(x' + \alpha (x - x'))}{\partial x_i} d \alpha,$$

where $F$ is the model relating the input vector $x$ (our averaged heartbeat) to an output $F(x)$ (age).

We calculated integrated gradients for every pixel in every channel for each of the 6044 subjects. We then summarized this information by aggregating information within channels and pixels across subjects. For each channel and pixel, we took the absolute value of the integrated gradient, then averaged those absolute values across subjects. We decided to take the absolute value before averaging so that important effects in opposite directions would not cancel each other out when averaging across subjects. The following figures show the average absolute integrated gradient intensity (color scale) against an averaged single heartbeat.

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 0](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan0.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 1](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan1.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 2](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan2.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 3](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan3.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 4](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan4.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 5](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan5.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 6](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan6.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 7](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan7.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 8](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan8.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 9](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan9.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 10](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan10.png)

![Average absolute integrated gradient intensity plotted against an averaged single heartbeat, Channel 11](https://github.com/sanhitamj/ecg_unravel/blob/integrated_gradients_writeup/output/images/integrated_gradients_chan11.png)

These images show high intensity in the P wave, T wave, and throughout the QRS complex. Notably, they also show intensity in the interval between the QRS complex and the T wave.