# Integrated Gradients Analysis

The integrated gradients analysis works with the one-beat dataset, which consists of 6044 subjects with consistent heartbeats. See `average_beat_pixel_removal.md` for a full explanation of how these 6044 subjects were selected. For each of those subjects, all hearbeats are averaged together to obtain a single representative heartbeat in each channel.

The analysis was implemented using the `IntegratedGradients` class in the `captum` package. Integrated gradients were introduced in 2017 as a way to identify which pixels of an image (or any input) contribute most strongly to the output generated by a model @sundararajan_axiomatic_2017. In order to calculate integrated gradients, we start with a baseline input $x'$. @sundararajan_axiomatic_2017 recommends using a black image or zero embedding for image and text analysis, respectively. We use a flatline heartbeat as our baseline input. Once we have a baseline, integrated gradients are calculated for a pixel $i$ in an input vector $x$ using the formula

$$IntegratedGrads_i(x) = (x_i - x'_i) \int_0^1 \frac{\partial F(x' + \alpha (x - x'))}{\partial x_i} d \alpha,$$

where $F$ is the model relating the input vector $x$ (our averaged heartbeat) to an output $y$ (age).

